mixin pages-list(items)
    ul.index-pages-list
      each item, index in items
        -
            const {
                name,
                url,
            } = item;
        li
            a(href=url)
                !=name
